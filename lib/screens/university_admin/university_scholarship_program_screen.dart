import 'package:flutter/material.dart';

class UniversityScholarshipProgramScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Bolsas de Estudo: Gerenciar aceitação de bolsas de estudo.',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
