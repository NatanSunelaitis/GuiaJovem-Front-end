import 'package:flutter/material.dart';

class UniversityAdmissionProgramScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Programas de Entrada: Gerenciar aceitação de programas de entrada.',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
