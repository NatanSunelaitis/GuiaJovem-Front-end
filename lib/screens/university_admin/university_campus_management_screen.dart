import 'package:flutter/material.dart';

class UniversityCampusManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Gerenciamento de Campus: Adicionar, editar e excluir campus.',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
