import 'package:flutter/material.dart';

class AdminFinancingProgramScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Gerenciamento de Programas de Financiamento: Adicionar, editar e excluir programas de financiamento.',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
