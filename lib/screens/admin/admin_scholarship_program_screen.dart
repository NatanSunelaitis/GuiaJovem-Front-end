import 'package:flutter/material.dart';

class AdminScholarshipProgramScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Gerenciamento de Programas de Bolsa de Estudo: Adicionar, editar e excluir programas de bolsa de estudo.',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
